ABC

move everything from TimeSeries up
TimeSeries will inherit from that
ArrayTimeSeries would re-implement


# see issue #41
The functions that are not in TimeSeriesInterface -- we'll define them in the SizedContainer (cut and paste)
1. repr()
2. str()
3. iter()
4. contains()
5. itertimes()
6. iteritems()
7. itervalues()
8. items()
9. identity()
10. lazy()
11. pos()
12. sub()
13. eq()


the following functions will have the @abc decorator
init(): now checks sizes of times and values
getitem(): uses np.take() which is faster than regular indexing (says the internet)
setitem(): uses np.put() likewise faster than regular indexing/setting
len(): uses np.size()
values: just returns the values (doesn't need to cast into a np array again)
times: just returns the times (doesn't need to cast into a np array again)
interpolate: now uses np.searchsorted() instead of the binary_search code
neg(): uses numpy multiplication
abs(): uses numpy absolute value
bool(): uses np.count_nonzero()
add(): uses numpy addition
mul(): uses numpy multiplication
eqtimes(): uses np.array_equal()
eqvalues(): uses np.array_equal()